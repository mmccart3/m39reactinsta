const Box = ({personsname, age, character}) => {
//   const changeName = () => {
//      setUser("Harry");
//    }
//   changeName();
return (
    <h1>{personsname} and their age is {age} and they are a {character}</h1>
)
};

export default Box;